# Pre-final empirical archive

This branch preserves the complete historical project state immediately
before the final empirical rebuild.

It includes:

- the full `17j` notebook history;
- unpublished local work including `18n`–`18y`;
- intermediate diagnostics and outputs;
- superseded empirical procedures;
- the complete development lineage before the clean final branch.

The supervisor-facing implementation is maintained separately on:

`edward-final-empirical-2026`
