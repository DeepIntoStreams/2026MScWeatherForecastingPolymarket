# Final pipeline raw data

Authoritative final-pipeline raw inputs are organised under:

- `hko/`
- `ecmwf/`
- `polymarket/`

Raw files must never be manually edited after acquisition.

Each acquisition stage will generate provenance metadata and integrity checks.
